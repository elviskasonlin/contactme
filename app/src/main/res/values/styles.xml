<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/colorControlNormal</item>
        <item name="colorControlActivated">@color/colorControlActivated</item>
        <item name="colorControlHighlight">@color/colorControlHighlight</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
    </style>

    <!-- Small hack to override global textColorPrimary for debug dialogue -->
    <style name="DebugDialogue" parent="@android:style/Theme.Dialog">
        <item name="android:textColor">@color/dialogTextColor</item>
        <item name="android:textColorPrimary">@color/dialogTextColor</item>
    </style>

</resources>